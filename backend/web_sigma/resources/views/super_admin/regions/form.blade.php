<div class="two-column"><label>Nama<input name="name" value="{{ old('name',$region->name ?? '') }}" required></label><label>Kode<input name="code" value="{{ old('code',$region->code ?? '') }}"></label><label>Level<select name="level" required>@foreach(['province','regency','district'] as $level)<option value="{{ $level }}" @selected(old('level',$region->level ?? '') === $level)>{{ ucfirst($level) }}</option>@endforeach</select></label><label>Induk<select name="parent_id"><option value="">Tidak ada</option>@foreach($parents as $parent)<option value="{{ $parent->id }}" @selected((string) old('parent_id',$region->parent_id ?? '') === (string) $parent->id)>{{ $parent->name }}</option>@endforeach</select></label><label>Luas area<input type="number" step="0.01" min="0" name="area_size" value="{{ old('area_size',$region->area_size ?? '') }}"></label></div>

<div class="two-column"><label>Nama<input name="name" value="{{ old('name', $organization->name ?? '') }}" required></label><label>Tipe<select name="type" required>@foreach(['government','team','company'] as $type)<option value="{{ $type }}" @selected(old('type', $organization->type ?? '') === $type)>{{ ucfirst($type) }}</option>@endforeach</select></label><label>Wilayah<select name="region_id"><option value="">Tanpa wilayah</option>@foreach($regions as $region)<option value="{{ $region->id }}" @selected((string) old('region_id', $organization->region_id ?? '') === (string) $region->id)>{{ $region->name }}</option>@endforeach</select></label><label>Status<select name="status" required>@foreach(['active','inactive'] as $status)<option value="{{ $status }}" @selected(old('status', $organization->status ?? 'active') === $status)>{{ ucfirst($status) }}</option>@endforeach</select></label><label>Telepon<input name="phone" value="{{ old('phone', $organization->phone ?? '') }}"></label><label>Email<input type="email" name="email" value="{{ old('email', $organization->email ?? '') }}"></label></div><label>Alamat<textarea name="address">{{ old('address', $organization->address ?? '') }}</textarea></label>
